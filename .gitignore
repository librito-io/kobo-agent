# Go workspace file is machine-local — do not commit (go convention).
go.work
go.work.sum

# Compiled agent binaries (cross-compiled per-arch, not source).
/dist/
librito-kobo-agent
*.test

# branch-review tooling artifacts
.claude/

# Design specs and implementation plans (local only)
docs/superpowers/
# Build plan — local-only working doc (device facts + step tracker), like the specs/plans above
docs/agent-build-plan.md
